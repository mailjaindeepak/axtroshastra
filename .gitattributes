# Normalize line endings to LF in the repo (fixes CRLF churn from Windows checkouts)
* text=auto eol=lf

# Explicitly treat binaries so git never touches their bytes
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.db binary
