[pytest]
# Put the repo root on sys.path so tests/conftest.py can `import api` whether
# they're run as `pytest` (console script) or `python -m pytest`. Without this,
# the bare `pytest` command fails at conftest import with "No module named 'api'".
pythonpath = .
testpaths = tests
