option_settings:
  aws:elasticbeanstalk:command:
    # pyswisseph / Pillow / reportlab build from source on Python 3.14 (no wheels),
    # which takes ~10 min. Default command timeout is 600s; raise it so the build
    # can finish during deployment.
    Timeout: 1800
