# Installed OFFLINE from the prebuilt wheelhouse in packages/ (Linux aarch64 / cp312).
# The EB instance does NOT reach PyPI and does NOT compile anything.
# To change deps: edit requirements.build.txt, then rebuild packages/ with build-wheels.bat.
--no-index
--find-links packages/
fastapi==0.115.*
uvicorn==0.30.*
pyswisseph==2.10.*
pydantic==2.*
setuptools<81       # razorpay imports pkg_resources at import time; removed from setuptools 81+ and absent from Py3.12 venvs
razorpay==1.4.*
PyMySQL==1.1.*
twilio
xhtml2pdf>=0.2.16          # (#7) server-side PDF rendering (HTML -> PDF)
# Optional: only needed for non-Indian birthplaces (#1 timezone resolution)
# timezonefinder>=6.5
geonamescache        # (#city) offline city gazetteer: autosuggest + coords + IANA tz
tzdata               # (#city) zoneinfo tz database for historical/DST birth offsets
