fastapi==0.115.*
uvicorn==0.30.*            # plain uvicorn (no uvloop/httptools/watchfiles source builds on Py3.14)
pyswisseph==2.10.*
pydantic==2.*
razorpay==1.4.*
PyMySQL==1.1.*          # MySQL driver for AWS RDS (dbcompat backend)
twilio
xhtml2pdf>=0.2.16          # (#7) server-side PDF rendering (HTML -> PDF)
# Optional: only needed for non-Indian birthplaces (#1 timezone resolution)
# timezonefinder>=6.5
